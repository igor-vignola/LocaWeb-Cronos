# Cronos — Challenge FIAP 2026 com Locaweb
# Dependências Python — AED + Feature Engineering + Modelagem (Sprint 3)
#
# Instalar:
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt   (Windows)
#   .venv/bin/python     -m pip install -r requirements.txt   (Linux/Mac)
#
# Python testado: 3.13.2 (Windows 11). Versões abaixo são as que rodam de fato.
#
# ⚠ NÃO adicionar `jupyter`, `jupyterlab`, `notebook` nem `ipywidgets`:
#   eles trazem "labextensions" com nomes de arquivo enormes que estouram o
#   limite de 260 caracteres do Windows (Long Path) e abortam a instalação
#   INTEIRA. Rodamos notebooks headless via nbconvert/nbclient + ipykernel,
#   que não precisam do JupyterLab. Ver context/conhecimento/.

# --- Manipulação de dados ---
pandas==2.2.3
numpy==2.2.4
openpyxl==3.1.5

# --- Visualização ---
matplotlib==3.10.1
seaborn==0.13.2
plotly==6.0.1
kaleido==0.2.1          # export estático de figuras Plotly (PNG/SVG p/ PPT)

# --- Calendário / sazonalidade ---
holidays==0.97          # feriados BR para a feature is_feriado

# --- Modelagem: forecast (Prophet) ---
# ⚠ Windows PT-BR: cmdstanpy quebra ao ler saída do `where.exe tbb.dll` (cp850 vs
#   utf-8). Fix = pôr a pasta tbb do cmdstan no PATH antes de usar o Prophet.
#   Já embutido no helper de modelagem. Ver context/conhecimento/.
prophet==1.3.0
cmdstanpy==1.3.0
stanio==0.5.1

# --- Modelagem: risco de OLA (XGBoost) + estatística ---
xgboost==3.0.2
scikit-learn==1.6.1
scipy==1.18.0
statsmodels==0.14.6

# --- Clusterização de séries temporais (DTW) ---
tslearn==0.9.0
numba==0.66.0
llvmlite==0.48.0

# --- Explicabilidade ---
shap==0.52.0

# --- Execução de notebooks (headless, SEM JupyterLab — ver aviso acima) ---
ipykernel==7.3.0
nbformat==5.10.4
nbconvert==7.17.1
nbclient==0.11.0

# --- Django (Fase 3 do MVP — será usado ao ligar previsões na aplicação) ---
# django==5.2.5         # descomentar quando iniciarmos o app
