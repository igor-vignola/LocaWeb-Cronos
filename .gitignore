# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
.ipynb_checkpoints/


# Modelos treinados (ficam grandes)
*.pkl
*.h5
*.joblib
models/

# Variáveis de ambiente e credenciais
.env
.env.local
*.key
secrets.json

# IDEs e editores
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local settings (preferências individuais da máquina)
.claude/settings.local.json


# Logs e temporários
*.log
*.tmp
tmp/
.cache/

# Office lock files (criados quando .pptx/.docx/.xlsx estão abertos)
~$*

# Docker (volumes locais)
docker/volumes/

# Django (vai aparecer na Sprint 3)
*.sqlite3
db.sqlite3
staticfiles/
media/
# Laboratório de modelagem (não comitado — vira notebook limpo no fim)
notebooks/lab/

# Backups de editor e de mão. Vinham sujando o `git status` desde a reconstrução do
# webapp — `hoje-v1.html.bak` é a versão intermediária de uma aba que já não existe.
*.bak
*.orig
*.rej

# Saídas temporárias de análise
data/**/_tmp_*
_tmp_*

# Ruído de sistema de arquivos
.DS_Store
Thumbs.db
desktop.ini

# Worktrees do Claude Code — estava só em .git/info/exclude, que não vai para o remoto
.claude/worktrees/

# PNG dos slides do deck: derivado do HTML ao lado, regenerado por
# scripts/monta_deck_sprint3.py. Sao 22 MB que nao precisam de historico.
sprints/*/slides/png/
